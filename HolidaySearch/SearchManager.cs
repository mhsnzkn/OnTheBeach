using SearchLayer.Models;

namespace SearchLayer
{
    public class SearchManager
    {
        public List<SearchResult> Filter(SearchRequest request)
        {

            return new List<SearchResult>();
        }
    }
}